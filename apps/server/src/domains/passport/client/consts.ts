import type { PassportAuthStatus } from './types';

export const PASSPORT_DEFAULT_AUTH_STATUS: PassportAuthStatus = 'disconnected';

export const PASSPORT_SYNC_FAILED_MESSAGE = 'Failed to sync passport status.';
export const PASSPORT_RETRY_FAILED_MESSAGE = 'Failed to retry passport status.';
