import { terminalAgentManifest } from './terminal-agent/manifest'
import { transcribeManifest } from './transcribe/manifest'
import { weatherManifest } from './weather/manifest'

export const TERMINAL_AGENT_WIDGET_ID = terminalAgentManifest.id
export const TRANSCRIBE_WIDGET_ID = transcribeManifest.id
export const WEATHER_WIDGET_ID = weatherManifest.id

